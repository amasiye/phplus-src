<?php

namespace My\App\Store;

final class ShoppingCartItem<TProduct> implements CartItem
{
    public function __construct(
        public private(set) int $id,
        public private(set) TProduct $product,
    ) {
    }
}
