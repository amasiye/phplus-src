# PHPStan is a pinned, replaceable ++PHP analysis backend. It does not define
# ++PHP language semantics or make its rule levels part of the language.
#
# Later stages will pass generated analysis-PHP paths to this independent base
# configuration. No generated path is mandatory during Stage 0.
parameters:
    level: max
